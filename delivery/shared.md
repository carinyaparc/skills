# Delivery — shared artefact contract

## Artefact

`delivery-plan.md` — sequences Phase-0 artefacts before the foundation sprint.

## Related skills

Invoke in order during **plan** mode: `product` write → `solution` write → `roadmap` write → `backlog` write → `contracts` write.
